<%= form_for(@masterbank) do |f| %>
  <% if @masterbank.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@masterbank.errors.count, "error") %> prohibited this masterbank from being saved:</h2>

      <ul>
      <% @masterbank.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :image %><br>
    <%= f.text_field :image %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
